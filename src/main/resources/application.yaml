pkslow:
  jwt:
    secretKey: daf66e01593f61a15b857cf433aae03a005812b31234e149036bcc8dee755dbb
    validityInMs: 86400000

server:
  port: 8080

spring:
  main:
    allow-circular-references: true

  datasource:
    url: jdbc:mysql://172.19.222.29:3306/future_cource?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: appuser
    password: App@123456Strong!
    driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

  mybatis-plus:
    mapper-locations: classpath:/mappers/*.xml

    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

  data:
    redis:
      host: 172.19.222.29
      port: 6379
      password: WLKCxm159357
      database: ${REDIS_DB:0}
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 16
          max-idle: 8
          min-idle: 2
          max-wait: 3000ms


neo4j:
  uri: bolt://172.19.222.29:7687
  username: neo4j
  password: WLKCxm159357


graph:
  limit:
    # 图谱构建最大层级深度 (对应 Excel 的列数 0-4)
    max-depth: 5
    # 单个图谱最大节点数量限制 (熔断保护)
    max-nodes: 2000



# MinIO配置 (新增)
minio:
  endpoint: http://8.152.202.189:9000  # MinIO服务地址
  access-key: 9eBv4Rt6                 # 访问密钥
  secret-key: 3a7W5Mz8                 # 秘密密钥
  bucket: homework-bucket              # 存储作业的桶


dify:
  api:
    url: https://api.dify.ai/v1
    homeworkkey: app-NtrVGVXmOaVZq4PkwY3p9WUQ
    expkey: app-NtrVGVXmOaVZq4PkwY3p9WUQ
    questiongenekey: app-X3SHHwb1or8lCbX9LOO9vD6k
    chatkey: app-4I5IOmfhuWYok3YMpPmzUKrD
    # richmediakey: app-jRN6m54W3Pn8b2zHwyjVoumm
    Databasekey: dataset-vdLI5IiCOMJ4ZyUyuBv6i0F4

rag:
  deepseek:
    api-key: ${DEEPSEEK_API_KEY:sk-3249b31bef32479cba07e8eff92829e9}
    base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com/v1}
    chat-model: ${DEEPSEEK_CHAT_MODEL:deepseek-chat}
    embedding-model: ${DEEPSEEK_EMBEDDING_MODEL:text-embedding-3-small}
  pgvector:
    enabled: ${RAG_PGVECTOR_ENABLED:false}
  embedding:
    provider: ${RAG_EMBEDDING_PROVIDER:local-model}
    dimension: ${RAG_EMBEDDING_DIMENSION:512}
    local:
      model-path: ${RAG_EMBEDDING_LOCAL_MODEL_PATH:C:/Users/Administrator/models/bge-small-zh-v1.5}
      tokenizer-path: ${RAG_EMBEDDING_LOCAL_TOKENIZER_PATH:C:/Users/Administrator/models/bge-small-zh-v1.5}
  document-index:
    path: ${RAG_DOCUMENT_INDEX_PATH:./data/rag-documents.json}

cache:
  ttl:
    course-list-seconds: 300
    course-detail-seconds: 300
    homework-list-seconds: 300
    homework-detail-teacher-seconds: 300
    class-list-seconds: 300
    question-list-seconds: 300
    graph-detail-seconds: 120
